# Orakel - Polymarket Trading Bot
# All values validated via Zod (src/env.ts) — see defaults there.

# Mode: true = paper trading, false = real trading
PAPER_MODE=true

# Wallet private key (64 hex chars, with or without 0x prefix)
# If set, wallet auto-connects on startup — no web UI needed.
# PRIVATE_KEY=

# Bearer token for mutation endpoints. Generate: openssl rand -hex 32
# API_TOKEN=

# Comma-separated active markets (default: all). Example: BTC,ETH
# ACTIVE_MARKETS=

# Log level: debug | info | warn | error | silent
# LOG_LEVEL=info

# Polygon RPC (default: https://polygon-rpc.com)
# POLYGON_RPC_URL=
# POLYGON_WSS_URL=
