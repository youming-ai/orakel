# Orakel - Polymarket Trading Bot
# Validated at startup via Zod (src/env.ts) — invalid values cause fail-fast exit.

# ── Core ──────────────────────────────────────────────────

# Mode: true = paper trading, false = real trading
PAPER_MODE=true

# API Server Port (default: 9999)
# API_PORT=9999

# Active markets (comma-separated, default: all). Example: BTC,ETH
# ACTIVE_MARKETS=

# ── Auth ──────────────────────────────────────────────────

# Bearer token for mutation endpoints (POST/PUT). If empty, auth is disabled.
# Generate: openssl rand -hex 32
# API_TOKEN=

# ── Storage ───────────────────────────────────────────────

# Persist backend: csv | dual | sqlite (default: sqlite)
# PERSIST_BACKEND=sqlite

# Read backend: csv | sqlite (default: sqlite)
# READ_BACKEND=sqlite

# ── Polymarket ────────────────────────────────────────────

# POLYMARKET_SLUG=
# POLYMARKET_AUTO_SELECT_LATEST=true
# POLYMARKET_LIVE_WS_URL=wss://ws-live-data.polymarket.com
# POLYMARKET_UP_LABEL=Up
# POLYMARKET_DOWN_LABEL=Down

# ── Polygon / Chainlink ──────────────────────────────────

# Primary RPC (default: https://polygon-rpc.com)
# POLYGON_RPC_URL=https://polygon-rpc.com

# Additional RPCs (comma-separated)
# POLYGON_RPC_URLS=

# WebSocket RPC
# POLYGON_WSS_URL=

# Additional WebSocket RPCs (comma-separated)
# POLYGON_WSS_URLS=

# Override BTC/USD aggregator address
# CHAINLINK_BTC_USD_AGGREGATOR=

# ── Logging ────────────────────────────────────────────────

# Log level: debug | info | warn | error | silent (default: info)
# LOG_LEVEL=info
