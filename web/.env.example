# Web service environment variables
# For production Docker builds, these are passed as build args

# API base URL - for production Docker builds, use internal Docker network address
# For local development, leave empty to use Vite proxy
# VITE_API_BASE_URL=http://localhost:9999

# Optional: API token for authentication (if backend requires it)
# VITE_API_TOKEN=your-secret-token
